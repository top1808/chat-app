import React from 'react';

type Props = {
  name: string;
};

const LoginComponent = (props: Props) => {
  console.log('🚀 ~ file: index.tsx ~ line 6 ~ LoginComponent ~ props', props);
  return <div>23</div>;
};

export default LoginComponent;
